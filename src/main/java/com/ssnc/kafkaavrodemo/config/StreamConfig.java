package com.ssnc.kafkaavrodemo.config;

import org.springframework.context.annotation.Configuration;

/**
 * Spring Cloud Stream configuration.
 * Most configuration is done via application.yaml.
 * This class can be used for any programmatic configuration if needed.
 */
@Configuration
public class StreamConfig {
    // Configuration is primarily done via application.yaml
    // Add custom beans here if needed
}

